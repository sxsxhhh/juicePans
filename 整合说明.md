# 网盘搜整合说明（给人看，Agent 执行搜索时不必读）

日期：2026-09-15 精修定稿；2026-09-16 增补引擎整合（1.3.0）、状态机移植（1.4.0）、更名与精简（1.4.2）、第二轮调研合入（1.5.0）、第三轮 11 项目合入与全面清理、性能小改（**现版本 1.6.2**，更名：原 pan-search → 果汁搜盘，技能标识 `juicePans`）
产物：本目录（技能名 `juicePans`，对外名 **果汁搜盘**，版本 **1.6.2**——1.2.1 沙箱适配 + panxiaozi / ghspider 引擎 + 四级链接状态机 + 综合排序 / 引擎熔断 / 关键词变体兜底 + 8 类内置链接核验 / 失效链沉底 / 限流退避）
来源：**Cursor 整合版（1.1.0）** + **WorkBuddy 整合版（6 引擎）** 合并，取双方精华、去其糟粕。
部署：整包复制为各端的 `skills/juicePans/`。不要写入别人维护的技能目录。

## v1.6.2（2026-09-16 性能诊断后小改）

逐引擎计时基准发现 pansou 公开站单源 44s（聚合 20+ 插件的固有延迟，并行下即总耗时）。两项改动：①搜索输出新增「各源耗时」行与 JSON `elapsed` 字段，慢源一眼可定位；②新增 `--pansou_timeout`（默认 45s），急用可调小，代价是聚合不全。

## v1.6.1（2026-09-16 第三轮调研 11 项目合入 + 全面清理）

按「与定位相符、能增强检索能力」逐项筛选（两批 6+5）：
- `check_links.py`：内置 8 类网盘匿名检测端点（夸克 CLI 兜底 / 阿里 / 115 / 123 / 天翼 / 百度 / 蓝奏 / UC，借鉴 fish2018/NetDiskLinkValidator，全标准库、无 cookie）；JSON 输出加统一状态码 `code` 与 `elapsed_ms`（借鉴 share-sniffer）。
- `search.py`：状态机「确认失效」链接搜索结果自动沉底（借鉴 quark-auto-save）；`ghspider` 引擎遇 GitHub 匿名 API 限流（429/403）按 Retry-After / 指数退避重试（借鉴 tg-index-enhanced）。
- `SKILL.md`：交付前失效候选自动换链（借鉴 CASX）、最佳匹配展示维度（借鉴 mediary-scout）、网盘风控频率纪律。
- **未合入**（备查）：funresource 自建采集库 / RSS 引擎（改动大）、PanSearch / ReMan 热词与搜索建议（输入侧体验、价值低）、share-sniffer 外部检测器接口（契约未逐条验证 + GPL v3）、qilin-Auto / Shiwen645（交集过小）。

## 为什么有这一版（取舍）

| 项目 | Cursor 1.1.0 | WorkBuddy 6引擎版 | 最终版（本目录）取了什么 |
|---|---|---|---|
| 结构 | 单文件 `search.py` + `check_links.py` | 6 个分引擎脚本 | **单文件 `search.py`**（更好移植/多端分发） |
| yunso | `opensearch.php` JSON 接口 | `search2` 爬 HTML | **JSON 接口**（更稳，已实测可用） |
| SSL | 先正常校验，失败再降级 | 全程关校验 | **先校验后降级**（Cursor 的，更安全） |
| pansou | 公开站 GET 优先 | res=merge | **GET 优先 + 兼容裸 merged_by_type**（两版一致） |
| 链接核验 | 只走自建 PanSou，不编造 | 夸克 CLI 兜底 | **两者合并**：夸克 CLI 实测 + 自建 PanSou + 其它诚实标「未核验」 |
| 网页兜底 | 交给 Agent 常规搜索 | 自带 films_search 爬虫 | **交给 Agent 的 WebSearch**（沙箱可用、更稳，弃用易碎爬虫） |
| 混合盘/15站 | 混合盘非默认；15站黑名单 | 15站全测不可用 | **全部不接入**（黑名单两版一致）；混合盘本沙箱实测 0 条，精修时已移除 |

## 本沙箱实测（2026-09-15/16，环境细节见沿革，不含在此）

| 引擎 | 结果 |
|---|---|
| pansou 公开（GET） | ✅ 真实 `merged_by_type` |
| yunso 小云 | ✅ JSON 真实直链 |
| haisou 海搜 | ⚠️ 可用但常 429，降级不中断 |
| movie 影视库 meng-ge | ❌ 502 被拦，仅 `--engine movie` 时试 |
| local 自建 PanSou | 取决于是否部署 |
| 夸克 CLI 核验 | ✅ `share-detail` 返回结构化 JSON，可判有效/失效 |

`--engine all` = `pansou,haisou,yunso`（不含 `movie` / `local`）。

## 纠错要点（两版共同修过的，本版保留）

1. 海搜不要传 `platforms:["all"]`（会 422）——脚本内已省略该字段。
2. 盘搜响应兼容裸 `merged_by_type` 与 `{code:0,data:...}`；默认 `res=merge`。
3. 网盘标识对外统一盘搜口径：`ali`/`alipan`→`aliyun`，`yidong`→`mobile`。
4. 纯标准库，无 `uv`/`requests` 硬依赖，无写死 Linux 家目录。
5. SSL 先正常校验，失败再降级；User-Agent 用浏览器兼容串。
6. 多源并行后按固定顺序合并、按 URL 去重；缺源只报告失败，不中断。
7. 小云搜索参数名必须是 `wd`（用 `keyword` 会触发人机验证）。

## 精修记录（2026-09-15 末轮）

- `all` 引擎集合纠错：原误含 `hunhepan`，与文档「all = 三源」不符 → 收敛为三源。
- 移除 `hunhepan` 引擎（本沙箱实测 0 条，且属用户「暂不加」范围），连带清掉 `HUNHEPAN_*` 常量与 `HUNHEPAN_TOKEN` 文档条目。
- 删除死代码：`HAISOU_PREFIX` 中未被使用的名称字段、`CLOUD_NAMES`/`TO_HAISOU` 里不可达的别名键、`clean_share_url` 里的空操作 `urlunsplit`。
- 文档纠错：删掉「脚本用 `PANSOU_USER`/`PANSOU_PWD` 登录」这一并不存在的能力；补上夸克 CLI 验链说明；清理残留的「引擎 C / 引擎 E」旧编号。

## 引擎整合记录（2026-09-16，用户提交 7 个 GitHub 仓库）

评估门槛：公开 HTTP 接口可直接调用 / 无登录墙 / 不违反站点使用条款 / 纯标准库可实现。逐仓库结论详见 `references/sources.md`「GitHub 同类项目评估」一节。

**整合 2 个：**

- `panxiaozi`（来源 [towelong/panxiaozi](https://github.com/towelong/panxiaozi)）：盘小子 SSR 搜索页 ld+json 取资源列表 → 并发抓详情页提取网盘直链 + 简介/分类/日期。修过一版 bug：首版正则会把详情页里「网盘入口按钮」的主站首页 URL（`pan.quark.cn` 等无 `/s/` 路径）误当分享链接，已改为只收真分享形态；详情页抓取失败显式报错、不静默吞。
- `ghspider`（来源 [John-h-netdisk/netdisk-spider](https://github.com/John-h-netdisk/netdisk-spider)）：TG 频道爬虫聚合库，仓库 `data/*.json` 每 6 小时自动更新；引擎经 GitHub contents API 拉最新文件、本地按关键词过滤。仅百度/夸克两类，数据量约 4 千条；匿名限流 60 次/时够用。

**不整合 5 个（记录结论防重复评估）：** 心悦搜索（无公开托管 API，与 yunso 同类无增量）、PanHub（公众号登录墙 + README 明确禁脚本，违反条款）、CloudSaver（需自建且新版闭源，转存型与定位不符）、supansou/DuPanSou（PanSou 增强壳，`PANSOU_URL` 已覆盖等价能力）、lzpan_search（2020 年前端壳，停滞）。

两个新引擎均为**非默认**：需显式 `--engine panxiaozi` / `--engine ghspider` 或组合指定。

## v1.4.0（2026-09-16）：supansou 状态机思路移植

supansou（DuPanSou-Archive）本体仍不整合（自建壳、数据同源 PanSou、API 不兼容），但其**链接四级状态机**借鉴进 `scripts/check_links.py`：

- 四级状态：有效 / 疑似失效（首败）/ 确认失效（连续 2 次失败才判死）/ 未核验
- 复检策略：有效 72h 内不复检；疑似 30min 内不重复检；失效 12h 后复查是否恢复
- 检测异常（uncertain）不计入失败；磁力/电驴永不误伤
- 状态存本地 JSON（默认 `~/.pan_search/link_state.json`），跨次生效；`--force` 强制实测、`--no-state` 关闭
- 已实测：真链→有效、假链→疑似失效（未判死）、二次运行走缓存、磁力不误伤，状态文件记录正确

## v1.5.0（2026-09-16）：第二轮同类项目调研借鉴

调研对象（GitHub 广搜，重点挑星标高、近期维护）：fish2018/pansou（约 8k★）、PanHub（1.6k★，已开源）、PanSeek（PanHub 分支，140+ TG 频道 / 68 插件）、Limitless-search（101 TG + 61 插件 + AI）。结论均记入 `references/sources.md`；四项借鉴已合入，结构与既有功能零回退：

1. **综合排序**（源：PanSou / PanSeek）：去重后组内按「关键词匹配度 > 来源等级（`ENGINE_RANK`）> 时间新鲜度」排序。
2. **引擎熔断**（源：PanSeek「失败插件自动降级 5 分钟」）：连续失败 2 次的引擎冷却 30 分钟，状态存 `~/.pan_search/engine_state.json`（`--fresh` 强制全跑；状态文件容错 BOM）。
3. **关键词变体兜底**（源：PanHub `searchKeyword` 思路）：0 结果自动用「去空格 / 全角转半角 / 去修饰词」变体重试一轮（`--no-variants` 关闭）。
4. **网盘类型域名表补全 + 抓取重试**（源：PanSeek 域名表 / PanHub `fetchWithRetry`）：URL 识别补 anxia、123 多域名、mypikpak、guangyapan（光鸭）；详情页抓取网络类异常退避重试 1 次。

「榜单 / 搜索建议」发现入口（源：PanHub 豆瓣榜单、Limitless-search 排行榜 / AI 建议）化为流程第 1 步指引，不新增代码。

## 目录

```text
juicePans/
  SKILL.md
  整合说明.md
  scripts/search.py
  scripts/check_links.py
  scripts/deploy.sh
  references/cloud-types.md
  references/sources.md
  references/pansou-api.md
  references/compliance.md
```
